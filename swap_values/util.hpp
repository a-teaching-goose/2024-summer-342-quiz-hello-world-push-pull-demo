#include <vector>
#include <iostream>

void swap(int &a, int &b);

template<typename T>
void swap(T *a, T *b) {
    /*
     * TODO: Swap the values pointed by the two int pointers a and b.
     *  Better yet, can you do this without a third variable?
     */
}
